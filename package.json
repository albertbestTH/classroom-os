{
  "name": "classroom-os",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:up": "docker compose up -d --wait postgres",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f postgres",
    "db:generate": "pnpm --filter @classroom-os/database db:generate",
    "db:format": "pnpm --filter @classroom-os/database db:format",
    "db:validate": "pnpm --filter @classroom-os/database db:validate",
    "db:migrate": "pnpm --filter @classroom-os/database db:migrate",
    "db:migrate:deploy": "pnpm --filter @classroom-os/database exec prisma migrate deploy",
    "db:migrate:status": "pnpm --filter @classroom-os/database db:migrate:status",
    "db:bootstrap:auth": "pnpm --filter @classroom-os/database db:bootstrap:auth",
    "db:seed:uat": "pnpm --filter @classroom-os/database db:seed:uat",
    "db:reset:uat": "pnpm --filter @classroom-os/database db:reset:uat",
    "db:verify:uat": "pnpm --filter @classroom-os/database db:verify:uat",
    "db:e2e:prepare": "pnpm --filter @classroom-os/database db:e2e:prepare",
    "db:e2e:cleanup": "pnpm --filter @classroom-os/database db:e2e:cleanup",
    "db:studio": "pnpm --filter @classroom-os/database db:studio",
    "db:test": "pnpm --filter @classroom-os/database db:test",
    "api:test": "pnpm --filter web test",
    "e2e": "pnpm --filter web e2e"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  }
}
